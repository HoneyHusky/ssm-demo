<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.by.mapper.UserMapper">
    <select id="queryUser" resultType="com.by.pojo.User">
     select * from t_user
  </select>
    <!--<insert id="addUser" parameterType="com.by.pojo.User">
  		INSERT INTO t_user (user_name,user_bir,user_hobby) VALUES(#{userName},#{userBir},#{userHobby})
  </insert>
    <update id="updateUser" parameterType="com.by.pojo.User">
  	update t_user set
  	user_name =#{userName} ,
  	user_bir =#{userBir},
  	user_hobby=#{userHobby}
  	where user_id=#{userId}
  </update>

    <delete id="deleUser">
  	delete * FROM T_USER WHERE USER_ID =#{userId}
  </delete>-->
</mapper>
